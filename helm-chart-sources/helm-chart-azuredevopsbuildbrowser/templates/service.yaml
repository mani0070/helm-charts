apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-azuredevopsbuildbrowser.fullname" . }}
  labels:
    {{- include "helm-chart-azuredevopsbuildbrowser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-azuredevopsbuildbrowser.selectorLabels" . | nindent 4 }}
